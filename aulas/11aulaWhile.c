#include <stdio.h>

int main(){

    int contador = 1;

    while (contador <= 5)
    {
       printf("%i\n", contador);
       ++contador; //adiciona no até chegar no 5, quant de vezes será o loop
    }
    
}